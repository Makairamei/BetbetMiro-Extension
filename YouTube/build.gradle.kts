version = 1

cloudstream {
    language = "id"
    authors = listOf("BetbetMiro")
    description = "YouTube provider with selected Indonesian channels, search-based categories, RSS/HTML parsing fallback, and Cloudstream extractor playback."

    status = 1

    tvTypes = listOf(
        "Movie"
    )

    isCrossPlatform = false
    iconUrl = "https://www.google.com/s2/favicons?domain=youtube.com&sz=%size%"
}
